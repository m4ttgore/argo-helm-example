apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-argo-app.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "test-argo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-argo-app.selectorLabels" . | nindent 4 }}
